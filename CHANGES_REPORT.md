# Mix 算法引擎 · 稳定性修复报告

修复日期：2026-07-29
基于文件：`MIX_backup_20260729_tar.gz`

本次一共改了 7 处，全部是这几轮对话里定位到的稳定性/正确性问题，没有动其他任何逻辑或 UI。逐条说明改了什么、为什么改、改动前后的行为差异。

---

## 1. 提示词模板顺序：题目 → 系数 → 答案

**文件**：`lib/engine/prompt_builder.dart`

**问题**：原顺序是 题目 → 答案 → 系数。药理学这类学科的"答案"经常需要展开机制、举例说明，篇幅不可控；一旦触发 `max_tokens` 截断，被砍掉的很可能是最后面的"系数"部分，而系数恰恰是喂给核心算法（掌握度更新）的关键数据。

**改动**：模板顺序改成 题目 → 系数 → 答案。系数在题目写完之后立刻给出（评估的是"这道题本身的难度"，不依赖答案内容），把截断风险转移到答案身上——答案truncate 顶多是这道题的解释不完整，不会污染掌握度模型。

**影响面**：只改了 prompt 文本，不影响已有数据。

---

## 2. AI 返回结果解析：顺序同步调整 + 提高 max_tokens

**文件**：`lib/services/ai_service.dart`

**改动**：
- `parseMarkdownResponse` 里三个 section 的抽取顺序、stop-pattern 跟着模板一起换成 `题目→系数→答案`。
- `max_tokens` 从 1024 调到 1536，进一步降低"答案"被截断的概率（系数已经不在答案后面了，这个调整主要是让答案本身更完整，不是稳定性刚需，算顺手的优化）。
- 答案抽取失败（截断到空）时不再存空字符串，改成存 `"(AI 未返回答案，请重新生成本题)"` 这种明确提示，比一道题空答案更容易被发现。

---

## 3. 系数解析改成"全有或全无"，不再半解析半填默认值

**文件**：`lib/services/ai_service.dart`、`lib/providers/app_state.dart`

**这是这几轮讨论里聊得最细的一个问题**，简单复述一下最终结论：

- 之前的写法是"只要四个系数里有一个不是 null 就进入差异化更新分支，缺的那几个用 `?? 0.5` 顶上"。
- 问题不在于"0.5 该不该是默认值"——0.5 作为默认值本身没问题，问题是**这个默认值触发的场景不对**。真正会出现"部分系数缺失"的场景，几乎只有一种：AI 本来四个都想给，但被截断或者格式没对齐导致正则少抠出来一两个。这种情况下拿 0.5 去顶，顶的是一次"解析事故"，不是一次"设计上认定的默认起点"，等于让一个格式抖动悄悄变成了看起来正常的训练数据，而且没有任何日志能追溯。

**改动后的行为**：
- `ai_service.dart`：四个系数只要有一个解析失败，`GeneratedQuestion` 里全部四个都存成 `null`（而不是"解析出来的存值，解析失败的存 null"这种混合态）。
- `app_state.dart`：`questionCoefs` 只有在四个系数都非 null 时才会被构造出来传给 `applyFeedback`；只要缺一个，就整体退化成原来的等量 bonus 模式（跟种子题、`MockAiService` 演示题目走一样的路径）。

**效果**：不会再出现"某个维度被一个不存在的 0.5 悄悄拉高"的情况。副作用是——系数不完整的题目这次答对不会享受"差异化更新"的好处，只能用老的 bonus 模式，但这是"退回到已知安全的旧行为"，比"用一个编造的值继续跑新逻辑"更保守也更可控。

---

## 4. `getOrCreateState` 的并发竞态

**文件**：`lib/repository/kp_state_repository.dart`

**问题**：原来是"查询是否存在 → 不存在就插入"，中间隔着一次 `await`。后台预生成（`PregenerationService`）是 fire-and-forget 调用，完全可能跟前台提交答案同时访问同一个 `(user_id, kp_id)`，两边都查到"不存在"再各自 insert，第二个会因为 `UNIQUE(user_id, kp_id)` 约束抛异常。

**改动**：把"查 + 插入 + 再查"整体包进 `db.transaction()`，insert 时加 `conflictAlgorithm: ConflictAlgorithm.ignore` 兜底——即使真撞上了并发，也是静默忽略插入、随后统一查询拿到最终那一行，不会抛异常。

---

## 5. 后台预生成失败不再是"未捕获异常"

**文件**：`lib/services/pregeneration_service.dart`

**问题**：`_triggerPregenerate` 里的 Future 只有 `.whenComplete()`，没有 `.catchError()`。AI 请求失败（网络问题、超时、解析异常等）会变成一个没人接的异常，用户完全不知道发生了什么，下次照样能重试，但这次失败全程没有任何痕迹。

**改动**：加了 `.catchError()`，失败时用 `dart:developer` 的 `log()` 记一条日志（包含 kp_id、异常、堆栈），然后继续走 `whenComplete` 清理 `_inFlight`。不改变"下次会自动重试"的行为，只是让失败变得可见、可查。

---

## 6. 四区调度的采样种子从 `kp_id` 改成 `question_index`

**文件**：`lib/engine/selection.dart`

**问题**：`pickZone(mastery, question_index)` 在设计文档里的本意是"同一个 mastery 值，在不同的题目序号下可能被采到不同的区"，靠这个制造软分区的随机性。之前的实现拿 `kp_id` 当种子，导致同一个知识点在同一个 mastery 值下会**永远**被分到同一个区——"维护穿插""安全穿插"每次抽到的候选池基本不会变化，软分区名存实亡。

**改动**：种子换成 `questionIndex * 1000003 + kp_id`（question_index 是主要因子，kp_id 只是同一批次内部的扰动项，避免所有知识点结果完全一样）。现在同一个知识点会随着做题进度在不同区之间自然漂移，跟设计文档的"确定性采样：同状态同输出"（这里的"状态"包含 question_index）保持一致。

---

## 7. `review_count` 恢复成"只在答对时累加"

**文件**：`lib/engine/feedback.dart`

**问题**：这次备份里把 `review_count += 1` 从"只在 `correct` 分支里"挪到了无条件执行，答错也会计数。`review_count` 会喂进艾宾浩斯衰减公式的 `1.3^review_count`（次数越多、S 越大、衰减越慢）。改成无条件累加之后，一个反复答错的知识点会因为"接触次数多"而被判定为"衰减更慢"，效果上跟"应该被更频繁地复习"的直觉是反着来的。

**改动**：改回设计文档原来的写法——`review_count` 只在 `if (correct)` 分支里累加。答错时不再影响这个计数器，衰减速度只会随着"真正答对的次数"变慢，跟间隔重复的基本假设（练熟了才能拉长间隔）对齐。

---

## 没有动的地方（说明一下为什么）

- **`EngineConstants.sMin` 从 0.02 改成 0.3**：这是你在这次备份里显式改的一个调参（注释也改成了"7小时半衰期"），看起来是有意为之的调整，不是意外改动，所以**没有改回去**。如果这不是你本意，告诉我一声，我再改。
- **`main.dart` 的三栏 UI（AI 聊天 / 刷题 / 文件管理）、`IDEA.md` 里的产品方向**：这些是你自己的产品设计延伸，不在"稳定性/bug"的讨论范围内，本次没有触碰。
- **`RadioGroup` 迁移、`WAL` 模式、`isOpen` 判断**：上次已经确认是正确的修复，本次也没有再改动。

---

## 建议之后验证的点

这些改动逻辑上是对的，但我这边没有 Flutter/Android 环境跑真实编译和单元测试，建议你在自己机器上：

1. `flutter analyze` 过一遍，确认没有类型错误（尤其是 `kp_state_repository.dart` 里 `db.transaction<T>` 的泛型推断）。
2. 用真实 AI Key 跑几轮出题，观察日志里有没有出现"预生成失败"的记录（验证第 5 点），以及系数是否稳定地"全有或全无"（验证第 3 点）。
3. 如果方便，写一个小单元测试固定 `mastery` 和几个不同的 `questionIndex`，验证同一个知识点确实会落到不同区（验证第 6 点的效果，而不只是代码逻辑上"看起来对"）。
